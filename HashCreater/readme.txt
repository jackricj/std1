this script will create a hash key for all the files inside the 
hashedDoc Folder. It will also create a txt file named as 
AllDocHashes outside this hashDoc folder 

AllDocHashes txt file will contain a list having name of file,
date when hash key is generated and first 10 digits of hashkeys
